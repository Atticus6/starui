import { withInstall } from "..//utils";
import {{capitalize componentName}} from "./{{componentName}}";


/**
 * {{description}}
 * 
 * @author {{usernmae}} <{{email}}>
 * @date {{createAt}}
 */
export const St{{capitalize componentName}} = withInstall({{capitalize componentName}});